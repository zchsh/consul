{{#if (gt this.item.Tags.length 0)}}
  {{#if (has-block)}}
    {{yield
      (component 'tag-list' item=this.item)
    }}
  {{else}}
    <dl class="tag-list">
      <dt>
        <Tooltip>
          Tags
        </Tooltip>
      </dt>
      <dd data-test-tags>
        {{#each this.item.Tags as |item|}}
          <span>{{item}}</span>
        {{/each}}
      </dd>
    </dl>
  {{/if}}
{{/if}}
