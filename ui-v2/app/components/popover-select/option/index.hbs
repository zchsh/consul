{{#let this.components.MenuItem as |MenuItem|}}
  <MenuItem
    class={{if this.selected 'is-active'}}
    ...attributes
    @onclick={{action 'click'}}
    @selected={{selected}}
  >
    <BlockSlot @name="label">
      {{yield}}
    </BlockSlot>
  </MenuItem>
{{/let}}
