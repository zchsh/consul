<EmberTooltip
  @targetId={{this.targetId}}
  @onHide={{this.oncomplete}}
  @isShown={{this.isShown}}
  @duration={{this.duration}}
  @event={{if this.isShown 'none' (or this.event 'hover')}}
  @popperContainer=".app-view"
  @side={{or this.position 'top'}}
>
  {{yield}}
</EmberTooltip>
