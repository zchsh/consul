{{yield}}
  <YieldSlot @name="content" @params={{block-params this.items}}>{{yield}}</YieldSlot>
{{#if (gt this.items.length 0)}}
  <YieldSlot @name="set" @params={{block-params this.items}}>{{yield}}</YieldSlot>
{{else}}
  <YieldSlot @name="empty">{{yield}}</YieldSlot>
{{/if}}