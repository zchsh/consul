<ConsulIntentionForm
  @nspace={{this.nspace}}
  @dc={{this.dc}}
  @src={{this.src}}
  @autofill={{hash
    SourceName=this.service
  }}
  @onsubmit={{transition-to 'dc.services.show.intentions.index'}}
/>
