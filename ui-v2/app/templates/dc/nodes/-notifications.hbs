{{#if (eq this.type 'delete')}}
  {{#if (eq this.status 'success') }}
    The session was invalidated.
  {{else}}
    There was an error invalidating the session.
  {{/if}}
{{else if (eq this.type 'update')}}
  {{#if (eq this.status 'warning') }}
    This node no longer exists in the catalog.
  {{else}}
  {{/if}}
{{/if}}
