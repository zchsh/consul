<div id="health-checks" class="tab-section">
  <div role="tabpanel">
    {{#if (gt this.item.Checks.length 0) }}
      <HealthcheckList @items={{sort-by (comparator 'check' 'Status:asc') this.item.Checks}} />
    {{else}}
      <EmptyState>
        <BlockSlot @name="body">
          <p>
            This node has no health checks.
          </p>
        </BlockSlot>
      </EmptyState>
    {{/if}}
  </div>
</div>