{{#let this.item.Services as |items|}}
<div id="services" class="tab-section">
  <div role="tabpanel">
  {{#if (gt items.length 0) }}
    <input type="checkbox" id="toolbar-toggle" />
    <SearchBar
      @placeholder="Search by name/port"
      @value={{this.search}}
      @onsearch={{action (mut this.search) value="target.value"}}
    />
  {{/if}}
     <ChangeableSet @dispatcher={{searchable 'nodeservice' items}} @terms={{this.search}}>
      <BlockSlot @name="set" as |filtered|>
        <ConsulServiceInstanceList @routeName="dc.services.show" @items={{filtered}} @checks={{this.checks}} />
      </BlockSlot>
      <BlockSlot @name="empty">
        <EmptyState>
          <BlockSlot @name="body">
            <p>
              There are no services.
            </p>
          </BlockSlot>
        </EmptyState>
      </BlockSlot>
    </ChangeableSet>
  </div>
</div>
{{/let}}